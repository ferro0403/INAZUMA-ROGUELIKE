"use strict";
const fs = require("fs");
const path = "tests/champion-hall-presentation-domain-test.js";
let source = fs.readFileSync(path, "utf8");
const from = 'assert.match(presentation.championFiveVFiveMarkup(snapshot), /Formazione salvata/);';
const to = 'assert.match(presentation.championFiveVFiveMarkup(snapshot), /1-2-1/);';
if (!source.includes(from)) throw new Error("missing champion 5v5 parity assertion");
source = source.replace(from, to);
fs.writeFileSync(path, source);
console.log("champion domain test parity assertion fixed");
