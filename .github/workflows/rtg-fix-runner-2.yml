name: RTG Fix Runner 2
on:
  push:
    branches:
      - feature/rtg-shop-development
    paths:
      - '.github/workflows/rtg-fix-runner-2.yml'
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ready
