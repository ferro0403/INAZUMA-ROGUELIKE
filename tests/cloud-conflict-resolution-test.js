'use strict';const assert=require('assert'),fs=require('fs'),P=require('../js/cloud-sync-protocol');const cloud=fs.readFileSync('js/firebase-cloud-save.js','utf8'),ui=fs.readFileSync('js/account-ui.js','utf8');assert.match(cloud,/resolveConflictUseLocal/);assert.match(cloud,/commitBundle\(uid, id, prepared, db, old, commitId\)/);assert.match(cloud,/cloud-cas-conflict/);assert.match(ui,/USA QUESTO DISPOSITIVO/);(async()=>{const s=P.memory(),base={revision:1,cloudCommitId:'a'};await P.publish({store:s,expected:null,commitId:'a',writes:[1],manifest:base});await P.publish({store:s,expected:base,commitId:'b',writes:[2],manifest:{revision:2,cloudCommitId:'b'}});await assert.rejects(P.publish({store:s,expected:base,commitId:'c',writes:[3],manifest:{revision:2,cloudCommitId:'c'}}),e=>e.code==='cloud-cas-conflict');console.log('cloud-conflict-resolution-test: ok')})().catch(e=>{throw e});
