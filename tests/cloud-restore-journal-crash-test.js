const assert = require('assert'); const vm = require('vm'); const fs = require('fs'); const BudgetStorage = require('./helpers/budget-storage');
for (let point = 0; point <= 8; point++) { const storage = new BudgetStorage(); const uid='u'; const journal={operationId:`r${point}`,uid,stage:['prepared','profile','run-ie1','runs','album','development','hall','verify','complete'][point]}; storage.setItem(`inazuma.cloud.restoreJournal.${uid}`,JSON.stringify(journal)); const c={localStorage:storage,module:{exports:{}},globalThis:null}; c.globalThis=c; vm.runInNewContext(fs.readFileSync('js/persistence-recovery-guard.js','utf8'),c); const g=c.module.exports; assert.equal(g.bindUid(uid).blocked,true); assert.throws(()=>g.assertWritable(),e=>e.code==='restore-recovery-required'); g.setBlocked({uid,operationId:journal.operationId,stage:'running'}); g.clearBlocked(journal.operationId); assert.equal(g.isBlocked(),false); }
console.log('cloud restore journal crash matrix R0-R8: ok');
