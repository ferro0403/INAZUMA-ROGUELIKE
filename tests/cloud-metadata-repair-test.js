const assert=require('assert'),fs=require('fs');const source=fs.readFileSync('js/firebase-cloud-save.js','utf8');for(const token of ['repairCommittedMetadata','hashesMatchManifest(manifest, prepared)','metadata-readback-failed','metadata-repair-needed'])assert(source.includes(token));assert.equal((source.match(/commitBundle\(uid, id, prepared, db, old/g)||[]).length,2);console.log('cloud metadata repair M0-M4 state machine: ok');
