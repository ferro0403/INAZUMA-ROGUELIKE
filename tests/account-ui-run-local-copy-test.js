'use strict';
const assert=require('assert'),fs=require('fs');const source=fs.readFileSync('js/account-ui.js','utf8');for(const copy of ['PROGRESSI ACCOUNT','PROGRESSI ACCOUNT SINCRONIZZATI','RIPRISTINA PROGRESSI ACCOUNT','Le run restano locali','run locale non è coinvolta'])assert.ok(source.includes(copy),copy);for(const forbidden of ['salvataggio cloud verrà creato quando inizierai una run','RIPRISTINA SALVATAGGIO','USA IL SALVATAGGIO CLOUD'])assert.ok(!source.toLowerCase().includes(forbidden.toLowerCase()),forbidden);console.log('account UI promises permanent-account sync only and identifies runs as local: ok');
