const assert=require('assert'),fs=require('fs'),P=require('../js/cloud-restore-protocol');const source=fs.readFileSync('js/firebase-cloud-save.js','utf8');assert.match(source,/InazumaCloudRestoreProtocol\.recover\(/);assert.doesNotMatch(source,/function applySnapshot/);let loads=0,applies=0;const journal=P.createJournal({operationId:'o',uid:'u',targetCloudRevision:1,targetCloudCommitId:'c',targetManifestIdentity:'i',sourceRunProvenance:{},targetRunHashes:{},sourceLocalEpoch:0,expectedLocalEpoch:0,startedAt:'n'}),snapshot={profile:{},runs:Object.fromEntries(P.RUN_IDS.map(x=>[x,null])),album:{},development:{},hallOfFame:{}};(async()=>{const result=await P.recover({journal,loadTarget:async()=>{loads++;return{snapshot,manifest:{revision:1}}},writeJournal:x=>x,clearJournal(){},adapters:{readRun:()=>null,runGeneration:()=>0,runEquals:(a,b)=>a===b,assertOwnership(){},applyRun(){applies++},storeEquals:()=>true,applyStore(){applies++},verify:()=>true,writeMetadata(){} }});assert.equal(result.status,'restored');assert.equal(loads,1);assert.equal(applies,0);console.log('firebase production wiring uses shared restore state machine: ok')})().catch(e=>{throw e});
