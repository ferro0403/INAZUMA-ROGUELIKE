'use strict';
// The cloud state machine's run-sector transitions were intentionally removed.
// Exercise the authenticated production runtime contract that run saves create
// neither dirty sectors nor revisions while account sectors remain active.
require('./cloud-update-local-provenance-test');
