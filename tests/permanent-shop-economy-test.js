const assert=require('node:assert/strict');
class Storage{constructor(){this.m=new Map()}getItem(k){return this.m.get(k)||null}setItem(k,v){this.m.set(k,v)}removeItem(k){this.m.delete(k)}}
global.localStorage=new Storage(); global.CustomEvent=class{}; global.dispatchEvent=()=>{}; global.SeasonRegistry={activeId:()=>"ie1"}; global.InazumaProgression={categoryForPotential:n=>n>=95?'Leggenda':n>=90?'Mondiale':n>=85?'Elite':n>=80?'Forte':n>=75?'Buono':n>=70?'Normale':n>=66?'Debole':'Scarso'};
const dev=require('../js/development-v2.js');
const end=(id,bosses,reason,seasonId='ie1')=>dev.processRunEnd({runId:id,defeatedBosses:bosses,endReason:reason,seasonId});
dev.reset(); end('loss3',3,'gameover'); assert.equal(dev.read().coins,60); end('loss5',5,'gameover'); assert.equal(dev.read().coins,160);
dev.reset(); end('win10',10,'victory'); assert.equal(dev.read().coins,300); assert.equal(dev.read().cupsBySeason.ie1,1); end('win10',10,'victory'); assert.equal(dev.read().coins,300);
dev.reset(); end('win11',11,'victory','ie1_s3'); assert.equal(dev.read().coins,320); assert.equal(dev.read().cupsBySeason.ie1_s3,1); assert.equal(dev.read().cupsBySeason.ie1,0);
let s=dev.read();s.coins=250;dev.write(s);assert(dev.purchaseProject('Buono').ok);assert.equal(dev.read().projects.Buono,1);assert.equal(dev.read().coins,0);assert(!dev.purchaseProject('Buono').ok);
s=dev.read();s.coins=600;s.cupsBySeason.ie1=1;dev.write(s);const epic={emblemId:'team:ie1:zeus',seasonId:'ie1',coins:600,cups:1};assert(dev.purchaseEmblem(epic).ok);assert(dev.read().unlockedEmblems.includes(epic.emblemId));assert(!dev.purchaseEmblem(epic).ok);
s=dev.read();s.coins=600;s.cupsBySeason.ie1=2;dev.write(s);assert.equal(dev.purchaseEmblem({emblemId:'team:ie1_s2:genesis',seasonId:'ie1_s2',coins:600,cups:1}).reason,'cups');
localStorage.setItem(dev.STORAGE_KEY,JSON.stringify({schemaVersion:5,cups:4,projects:{Elite:2},projectBuild:{Elite:3}}));const migrated=dev.read();assert.equal(migrated.legacyCups,4);assert.equal(migrated.projects.Elite,2);assert.equal(migrated.legacyProjectBuild.Elite,3);dev.write(migrated);assert.deepEqual(dev.read(),migrated);
s=dev.read();s.coins=800;s.legacyCups=1;s.cupsBySeason.ie1=1;s.cupsBySeason.ie1_s2=1;s.projects.Elite=1;dev.write(s);assert(dev.evolve({playerId:'p',basePotential:80,unlocked:true,freeAgentEligible:true,cupSelection:{ie1:2,ie1_s2:1}}).ok);assert.equal(dev.totalCups(),0);assert.equal(dev.read().cupsBySeason.ie1,0);assert.equal(dev.read().cupsBySeason.ie1_s2,0);
console.log('permanent-shop-economy-test: rewards, purchases, cups and migrations OK');
const catalogModule=require('../js/shop-catalog.js');
const databases={ie1:require('../data/IE1_season_compact.json'),ie1_s2:require('../data/IE1_S2_season_compact.json'),ie1_s3:require('../data/IE1_S3_season_compact.json'),ie2:require('../data/IE2_season_compact.json')};global.SeasonRegistry.database=id=>databases[id];const catalog=catalogModule.build();assert.equal(catalog.filter(x=>x.teamId==='zeus').length,1);assert(catalog.some(x=>x.teamId==='royal_academy_ares'&&x.rarity==='rare'));assert(catalog.some(x=>x.teamId==='barcelona_orb'&&x.cups===2));assert(!catalog.some(x=>x.seasonId==='ie2'&&['alpine','kirkwood'].includes(x.teamId)));
