"use strict";
const assert = require("assert"), B = require("./helpers/budget-storage"), { load } = require("./helpers/production-runtime");
(async () => { const uid = "firebase-uid-secret-123", device = "complete-device-id-secret", s = new B(), c = load(s, ["persistence-recovery-guard.js", "run-state.js", "album-progress.js", "development-v2.js", "hall-of-fame.js", "persistence-diagnostics.js"]); c.InazumaAccount = { getState: () => ({ status: "authenticated", uid, email: "private@example.test", token: "firebase-token-secret" }) }; c.InazumaCloudSave = { getState: () => ({ status: "synced", deviceId: device, cloudCommitId: "full-private-commit" }) }; s.setItem(`inazuma.cloud.restoreJournal.${uid}`, JSON.stringify({ uid, operationId: "full-operation-secret", stage: "prepared", targetCloudCommitId: "full-private-commit" })); s.setItem(`inazuma.cloud.association.${uid}`, JSON.stringify({ uid, deviceId: device, token: "firebase-token-secret" })); const text = JSON.stringify(await c.InazumaPersistenceDiagnostics.snapshot()); for (const secret of [uid, "private@example.test", "firebase-token-secret", device, "full-operation-secret", "full-private-commit"]) assert(!text.includes(secret), secret); assert(text.includes("inazuma.cloud.restoreJournal.[account]")); console.log("diagnostic JSON UID/email/token/device/operation/commit redaction: ok"); })().catch((error) => { throw error; });
