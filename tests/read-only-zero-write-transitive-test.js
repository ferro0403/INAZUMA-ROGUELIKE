const assert=require('assert'),B=require('./helpers/budget-storage'),{load}=require('./helpers/production-runtime'),core=require('../js/cloud-save-core');const s=new B(),c=load(s);let run=c.RunState.createRun({name:'R'},'ie1');run.permanentEffectOutbox=[{id:'p',status:'pending'}];c.RunState.save(run);c.RunState.saveProfilePreferences({smartAutoLineup:true});const before=JSON.stringify(run.permanentEffectOutbox),start=s.operations.length;c.RunState.load('ie1',{readOnly:true});c.RunStorage.diagnostics('ie1');core.readLocalSnapshot(c);const writes=s.operations.slice(start).filter(x=>x.method!=='getItem');assert.deepEqual(writes,[]);assert.equal(JSON.stringify(run.permanentEffectOutbox),before);console.log('read-only production RunState/diagnostics/cloud snapshot zero-write: ok');
