"use strict";
const assert=require("assert"),fs=require("fs"),vm=require("vm");
const c={structuredClone,Date,console};c.globalThis=c;vm.createContext(c);vm.runInContext(fs.readFileSync("js/boss-gameover-runtime.js","utf8"),c);
const seam=fs.readFileSync("js/app.js","utf8");for(const name of ["completeBossMatch","continueAfterMatch","resolvePendingRunFlow","advanceBossReward","finishBossVictoryTransition","navigateBossVictoryDestination","resumeRunFinalization"])assert.match(seam,new RegExp(`__INAZUMA_TERMINAL_FLOW_TEST__[\\s\\S]*${name}`));
const db={bossOrder:[{teamId:"barcelona"}]},node={id:"final",completed:false},run={runId:"final-flow",bossIndex:0,phase:"match",completedBossIds:[],unlockedTeamIds:[],currentZone:{nodes:[],completedNodeIds:[]},activeMatch:{matchId:"m",type:"boss",bossIndex:0,nodeId:"final",simulation:{resolutionApplied:false,score:{user:2,opponent:1}}},statistics:{}};run.currentZone.nodes=[node];
const deps={applyStatistics(){},addLevels(){},completeNode(z,id){z.nodes.find(n=>n.id===id).completed=true},restoreAfterLoss(){},lossToast(){},appendFinalMessage(){}};
c.BossGameOverRuntime.applyBossResolutionMutation({run,matchId:"m",result:"victory",seasonDb:db,deps});c.BossGameOverRuntime.applyPostBossResumeMutation({run,seasonDb:db,clearMatch:true});c.BossGameOverRuntime.advanceBossRewardMutation({run});c.BossGameOverRuntime.advanceBossRewardMutation({run});let snapshots=0;const out=c.BossGameOverRuntime.applyBossVictoryHandoffMutation({run,seasonDb:db,ensureCurrentZoneMutation(){},buildFinalization(){snapshots++;run.finalization={status:"pending"}}});
assert.equal(out.destination,"finalization-pending");assert.equal(run.bossIndex,1);assert.deepStrictEqual(run.completedBossIds,["barcelona"]);assert.equal(snapshots,1);const reload=structuredClone(run);assert.equal(reload.phase,"finalization");assert.equal(reload.finalization.status,"pending");console.log("real final boss production transition path: ok");
