"use strict";
const assert = require("assert"), B = require("./helpers/budget-storage"), { load } = require("./helpers/production-runtime");
const stages = ["before-guard-clear", "before-journal-remove"];
(async () => { for (const crashStage of stages) { const s = new B(), c = load(s, ["persistence-recovery-guard.js", "run-state.js", "album-progress.js", "development-v2.js", "hall-of-fame.js", "persistence-diagnostics.js"]), key = "inazuma.cloud.restoreJournal.A"; c.InazumaAccount = { getState: () => ({ status: "authenticated", uid: "A" }) }; s.setItem(key, JSON.stringify({ uid: "A", operationId: "op", stage: "prepared", sourceLocalEpoch: 0, expectedLocalEpoch: 0, targetCloudCommitId: null })); c.PersistenceRecoveryGuard.bindUid("A"); await assert.rejects(c.InazumaPersistenceDiagnostics.repair({ crash: (stage) => { if (stage === crashStage) throw Error("crash"); } })); assert(s.getItem(key), `journal retained at ${crashStage}`); c.PersistenceRecoveryGuard.bindUid("A"); assert.equal((await c.InazumaPersistenceDiagnostics.repair()).action, "aborted-unmodified-restore"); assert.equal(s.getItem(key), null); }
  const s = new B(), c = load(s, ["persistence-recovery-guard.js", "run-state.js", "album-progress.js", "development-v2.js", "hall-of-fame.js", "persistence-diagnostics.js"]), key = "inazuma.cloud.restoreJournal.A"; c.InazumaAccount = { getState: () => ({ status: "authenticated", uid: "A" }) }; s.setItem(key, JSON.stringify({ uid: "A", operationId: "op", stage: "prepared", sourceLocalEpoch: 0, expectedLocalEpoch: 0, targetCloudCommitId: null })); c.PersistenceRecoveryGuard.bindUid("A"); s.fail = { method: "removeItem", key }; await assert.rejects(c.InazumaPersistenceDiagnostics.repair()); s.fail = null; assert(s.getItem(key)); c.PersistenceRecoveryGuard.bindUid("A"); await c.InazumaPersistenceDiagnostics.repair(); assert.equal(s.getItem(key), null); console.log("safe repair crash/remove failure matrix and reload convergence: ok"); })().catch((error) => { throw error; });
