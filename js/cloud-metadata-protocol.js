(function(global){"use strict";async function repair({manifest,prepared,metadata,matches,build,write,read,publish}){if(!matches(manifest,prepared))return{handled:false};const next=build(manifest,prepared,metadata||{});try{write(next);const verified=read();if(verified?.revision!==manifest.revision)throw Error('metadata-readback-failed')}catch(error){publish?.({status:'sync-error',error:'metadata-repair-needed'});return{handled:true,status:'metadata-repair-needed',error}}publish?.({status:'synced',revision:manifest.revision,localRevision:manifest.revision});return{handled:true,status:'synced',metadata:next}}
const api=Object.freeze({repair});global.InazumaCloudMetadataProtocol=api;if(typeof module!=="undefined"&&module.exports)module.exports=api})(globalThis);
